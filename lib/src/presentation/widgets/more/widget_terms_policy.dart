import 'package:flutter/material.dart';

class TermPolicyWidget extends StatelessWidget {
  const TermPolicyWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
